# This file was auto-generated by Fern from our API Definition.

from ..core.pydantic_utilities import UniversalBaseModel
import pydantic
import typing
import datetime as dt


class PegOrderParams(UniversalBaseModel):
    """
    Parameters for creating a peg complex order.

    A peg's child limit order rests on the venue at the current best bid (for buys)
    or best ask (for sells), clamped to the absolute band [min_price, max_price]:
    the child never rests below min_price or above max_price. Both bounds are
    optional — an unset min floors at 0, an unset max ceils at 1. The peg worker
    repegs the child as the top of book moves.
    """

    buy_flag: bool = pydantic.Field()
    """
    Direction: true=buy, false=sell
    """

    total_qty: float = pydantic.Field()
    """
    Total peg quantity
    """

    min_price: typing.Optional[float] = pydantic.Field(default=None)
    """
    Price floor — the child never rests below this (between 0 and 1)
    """

    max_price: typing.Optional[float] = pydantic.Field(default=None)
    """
    Price ceiling — the child never rests above this (between 0 and 1)
    """

    post_only: typing.Optional[bool] = pydantic.Field(default=None)
    """
    If true, the resting child order is posted as post-only; a repeg that would cross the book cancels the peg instead of taking liquidity
    """

    expiry_ts_utc: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    Optional expiry timestamp in UTC (ISO 8601). When set, the resting child is submitted to the venue as GTD with this expiry; venue-side expiry cancels the peg (caught by the periodic reconciliation sweep).
    """

    peg_min_stay_time_s: typing.Optional[int] = pydantic.Field(default=None)
    """
    Optional repeg gate. When set, a price-driven repeg is suppressed until the resting child has been on the book for at least this many seconds (measured from send time). Expiry-driven cancels are unaffected.
    """

    max_qty_level: typing.Optional[float] = pydantic.Field(default=None)
    """
    Penny-jump threshold (contracts). If the level the peg would join holds more than this many contracts, rest one tick tighter instead (if not crossing the book and not already quoting the level). Unset to disable.
    """

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(
        extra="allow", frozen=True
    )  # type: ignore # Pydantic v2
