# This file was auto-generated by Fern from our API Definition.

from ..core.pydantic_utilities import UniversalBaseModel
import pydantic
import typing
import datetime as dt


class IcebergOrderParams(UniversalBaseModel):
    """
    Parameters for creating an iceberg complex order.

    An iceberg's `total_qty` is split into sequential `displayed_qty` tranches
    posted at the same `limit_price`. When a tranche fully fills, the next is
    placed automatically until the total is exhausted or the iceberg is cancelled.
    """

    buy_flag: bool = pydantic.Field()
    """
    Direction: true=buy, false=sell
    """

    total_qty: float = pydantic.Field()
    """
    Total iceberg quantity (across all tranches)
    """

    displayed_qty: float = pydantic.Field()
    """
    Tranche size — quantity shown to the market at any moment
    """

    limit_price: float = pydantic.Field()
    """
    Static limit price for every tranche (between 0 and 1)
    """

    post_only: typing.Optional[bool] = pydantic.Field(default=None)
    """
    If true, every tranche is posted as post-only (maker-only)
    """

    reload_delay_s: typing.Optional[int] = pydantic.Field(default=None)
    """
    Optional delay between a tranche fully filling and the next one being placed, in seconds. NULL or 0 = place next tranche immediately.
    """

    expiry_ts_utc: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    Optional expiry timestamp in UTC (ISO 8601). When set, every tranche is submitted to the venue as GTD with this expiry; venue-side expiry of a tranche cascades to cancel the parent iceberg.
    """

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(
        extra="allow", frozen=True
    )  # type: ignore # Pydantic v2
